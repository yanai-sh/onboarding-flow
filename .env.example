# Cloud Run (or local shell) environment variables — not loaded automatically by the app.
# Export manually when running outside Cloud Run, e.g.:
#   export UPSTREAM_URL=https://insurance-webhook-945894769129.us-central1.run.app/vehicle-info
#   export UPSTREAM_TIMEOUT_SECONDS=5.0

# GCP deploy uses infra/terraform/terraform.tfvars (see infra/README.md), not .env.
