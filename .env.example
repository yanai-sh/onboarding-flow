# Local settings. The app reads a .env file in the working directory (see
# src/onboarding_flow/config.py); real environment variables take precedence.
# Every setting is optional; the values below are the defaults.
UPSTREAM_URL=https://insurance-webhook-945894769129.us-central1.run.app/vehicle-info
UPSTREAM_TIMEOUT_SECONDS=5.0
LOG_LEVEL=INFO

# GCP deploy uses infra/terraform/terraform.tfvars (see infra/README.md), not .env.
